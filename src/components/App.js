import React from "react";
import AvatarNotifier from "./AvatarNotifier";

const App = () => {
  return (
    <div>
      <h1>{` User Dashboard `}</h1>
      <AvatarNotifier />
    </div>
  );
};

export default App;
