import React from "react";
import AvatarNotifier from "./AvatarNotifier";

const App = () => {
  return (
    <header>
      <h2>{` User Dashboard `}</h2>
      <AvatarNotifier />
    </header>
  );
};

export default App;
